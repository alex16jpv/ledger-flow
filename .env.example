# Server only. The browser never learns the backend URL; every call goes through the BFF.
API_URL=http://localhost:3000
# Server only. Shared secret the backend expects in `x-api-secret` (required in production, unset locally).
# API_SECRET=

# Public. Base URL of this deployment: metadata, canonical, sitemap, manifest.
NEXT_PUBLIC_APP_URL=http://localhost:3001
NEXT_PUBLIC_CONTACT_EMAIL=ledgerflow@alexpiral.com
# Set by the deploy pipeline (tag or commit SHA); shown in Settings > About.
NEXT_PUBLIC_APP_VERSION=dev
# Optional. Feature-flag environment when it differs from NODE_ENV (the e2e build sets "test").
# NEXT_PUBLIC_APP_ENV=test
# Optional. Worker the app registers; the e2e build overrides it together with NEXT_DIST_DIR and
# SERWIST_SW_DEST so it never overwrites the `.next` and the `public/sw.js` a running app serves (F-56).
# NEXT_PUBLIC_SW_PATH=/sw.js
# Build-time only, both defaulted: where `next build` writes and where `serwist build` puts the worker.
# NEXT_DIST_DIR=.next
# SERWIST_SW_DEST=public/sw.js

# Optional. Error tracking (Sentry, free plan). Events are anonymized in lib/observability/scrub.ts and
# tunnelled through /monitoring; without a DSN the SDK stays disabled.
# NEXT_PUBLIC_SENTRY_DSN=
# Build-time only, set in the deploy pipeline to upload source maps.
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=
